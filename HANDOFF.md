# 밥친구 / Eatple — 인수인계

**HEAD:** `388093c` · 2026-08-03 · 워킹트리 깨끗
**테스트:** 382개 전부 통과 (`npm test`)
**배포:** https://test-umber-phi-78.vercel.app

이 문서 하나로 이전 대화를 읽지 않고 이어서 작업할 수 있게 썼습니다.
숫자는 기억이 아니라 위 커밋의 저장소에서 직접 센 것입니다.

이 파일의 이전 버전은 **K-Food Map**이라는 다른 앱의 인수인계 문서였습니다.
그 내용은 옆 저장소(`rkdals0121/kfoodmap`)와 이 저장소의 git 이력
(`git show 31e4b2f:HANDOFF.md`)에 그대로 남아 있습니다.

---

## 0. 먼저 읽어야 할 경고

**이 폴더에서 `kfoodmap` 저장소로 절대 푸시하지 마세요.**
8월 2일에 실제로 그 일이 일어났고, 남의 라이브 앱이 몇 시간 동안 밥친구를
서빙했습니다. 지금은 원격이 하나뿐이고 pre-push 훅이 막지만, **새로 클론하면
훅을 다시 켜야 합니다:**

```bash
git config core.hooksPath .githooks
```

전말은 `docs/where-this-deploys.md`에 있습니다.

---

## 1. 이 앱이 무엇인가

**"Solo trip, Shared table."** 혼자 한국에 온 외국인 여행자가 한식에서
차단되는 이유는 언어도 가격도 아니고 **1인분이 없다는 것**입니다.
삼겹살은 2인분부터, 감자탕은 냄비 단위, 한정식은 2인 예약.

그래서 이 앱은 식당을 찾아주지 않습니다. **같이 먹을 사람이 있는 상**을
찾아줍니다. 혼자서는 주문 자체가 불가능한 요리를 걸고, 그 상에 자리를
요청하거나 직접 상을 엽니다.

상은 두 종류이고, 라벨은 주장할 수 없게 **파생**됩니다
(`src/domain/catalog/hosts.js`):

- **호스트 테이블** — 호스트가 문화 가이드를 하나 이상 켠 상
- **테이블 메이트** — 그냥 같이 먹는 상

`tableKind()`가 guides 배열에서 계산하므로, 가이드 없이 "호스트 테이블"로
표시되는 일이 구조적으로 불가능합니다.

### 화면 넷

`Explore` / `Tables` / `Places` / `Passport`.
Passport는 프로필과 합쳐져 있고 `/profile`은 별칭으로 남겨뒀습니다.

라우터 라이브러리 없이 History API로 아홉 개 경로를 씁니다 (`src/routes.js`).
`/tables/<id>`가 공유 링크입니다 — **`vercel.json`의 SPA rewrite가 없으면
프로덕션에서만 404가 납니다.** 개발 서버에서는 멀쩡해서 놓치기 쉽습니다.

---

## 2. 30분 안에 돌리기

```bash
npm install
cp .env.example .env.local
npm run dev
```

`.env.local`을 안 채워도 **앱은 돕니다** — localStorage로 떨어져서 한 대
안에서만 공유됩니다. 키가 필요하면 Supabase 대시보드 Project Settings → API에서
가져오고, **`.env.example`의 주석을 꼭 읽으세요.** 대시보드가 키 이름을 바꿔서
`sb_publishable_`(공개, 이게 들어감)과 `sb_secret_`(RLS 우회, **절대 금지**)를
헷갈리기 쉽습니다. `VITE_` 접두사가 붙은 값은 브라우저 번들에 그대로
컴파일됩니다.

새 Supabase 프로젝트에 붙일 때는 **먼저 `supabase/schema.sql`을 돌리세요.**
anon 키가 공개여도 안전한 이유가 RLS이고, 그게 이 파일에 있습니다.

```bash
npm test          # 382개
npm run lint      # oxlint
npm run build
```

**검증은 375×812에서 하세요.** 데스크톱 폭에서 확인하다가 놓친 문제가 실제로
있었습니다.

---

## 3. 이 저장소의 규칙 — 어기면 테스트가 잡습니다

이 프로젝트는 **국고가 들어간 공공외교 사업**이고, 외국인에게 한국에 대해
사실을 가르칩니다. 그래서 정직성 규칙이 문서가 아니라 **테스트로** 박혀
있습니다.

- **가격을 쓰지 않습니다.** 확인할 방법이 없습니다.
- **출처 없는 퀴즈 문제는 여행자에게 도달하지 않습니다.** `quizFor`가
  거릅니다. 파일에는 남고 화면에는 안 나옵니다.
- **샘플 데이터는 샘플이라고 표시**합니다.
- **요리에 대해 식이 판정을 내리지 않습니다.** 비건·할랄은 앱의 판정이 아니라
  **호스트에게 전달되는 메시지**입니다 (`src/data/profile.js`).
- **산문에 등장하는 재료는 `contains`에 선언**되어야 합니다. 빈 `contains`는
  `varies: true`를 함께 선언해야 합니다.

`src/content/sources.js`의 규칙이 특히 중요합니다: **등록된 출처는 전부 직접
열어서 읽은 것**입니다. 제목만 보고 넣지 마세요. 각 항목 주석에 근거가 된
문장이 인용돼 있으니 같은 형식으로 이어가면 됩니다.

이건 장식이 아닙니다. 10개 문제에 출처를 붙이는 과정에서 **앱이 출처보다 많이
말하고 있던 문장 4개**가 나왔습니다 — 12첩을 왕에게만 묶은 것, 고추 전래를
"16세기"로 단정한 것 등. 무엇을 왜 고쳤는지는 `docs/sources-status.md`에
있습니다.

---

## 4. 외부 피드백 네 건 — 대조표

이 앱이 다음에 무엇을 해야 하는지는 코드가 아니라 이 네 문서가 정합니다.
아래 표의 "상태"는 전부 위 HEAD의 저장소에서 확인한 것입니다.

- **신보람 교수님** (전북대 국제학부) — 세부사업계획 피드백
- **김훈 부장님** (한국과학창의재단 정보화지원부) — 세부사업계획 피드백
- **8/2 회의** — 팀 내부
- **외국인 친구들 8/1** — 실제 사용자 리뷰, 우리 URL을 직접 써보고 씀

### 가장 무거운 지적부터

**교수님, 종합의견:** 1인분 안 되는 메뉴를 나눠 먹고 비용을 나누는 데
그치면 **"실용적인 여행 앱일 뿐, 공공외교성은 잘 드러나지 않는다."**
그리고 방향을 줬습니다 — **"무엇을 하느냐"보다 "어떻게 하느냐."**

여기에 대한 부분적인 답이 이미 코드에 있습니다. **호스트 테이블**이 바로
교수님이 말한 내국인↔외국인 교류입니다. 한국인 호스트가 주문 방법·식사
예절·유래를 가이드로 걸고 외국인을 모읍니다. **그런데 앱은 그걸 그런
말로 설명하지 않습니다.** 기능은 있고 프레이밍이 없습니다.

이건 기능 추가가 아니라 **문장 문제**이고, 그래서 원격으로 할 수 있는
일 중 값어치가 가장 큽니다.

### 이미 반영된 것

| 요구 | 누가 | 어디에 |
| --- | --- | --- |
| 호스트 테이블 / 테이블 메이트 구분 | 8/2 회의 | `hosts.js` — 라벨이 가이드에서 파생 |
| 패스포트 + 프로필 합치기 | 8/2 회의, 외국인 | Passport 탭 |
| 테마 코스 완료 보상 | 8/2 회의, 외국인("no reward function") | `policy/completion.js` |
| 비건·할랄 옵션 | 외국인 | `profile.js` DIETS 4종 |
| 요리 이름 뜻 병기 (로마자만 쓰지 않기) | 외국인 | 메뉴 카탈로그 |
| 어색한 회화 문장 교정 | 외국인 | "이거 몇 인분이에요" |
| 폰 뒤로가기 · 공유 링크 | 외국인(technical) | `routes.js` + `vercel.json` |
| 음식의 유래·문화 정보 | 부장님 | 출처 18건, 퀴즈 24문제 |
| 첫 방문 안내 | 외국인("랜딩 혼란") | 8/4부터 랜딩의 3단계 스트립(`how-strip`, 게스트에게만) — Prologue 스플래시는 Meetup형 랜딩으로 대체되어 삭제됨 |
| 다크 모드 | 외국인("앱 쓸 때 중요하게 본다") | `index.css` `:root[data-theme="dark"]` — 토큰 값만 분기, 실기기에서 2건 추가로 발견·수정(백-패널 흰색 하드코딩, 마켓 카드 제목 파란 글씨) |
| 라이트/다크/시스템 직접 선택 | 사용자(폰으로 확인 후 요청) | `src/data/theme.js`, Profile의 Appearance 칩 3개 |
| 알러지 직접 입력 | 외국인("what if ur allergic to prawns") | Profile의 자유 텍스트 필드, `signups.allergy_note` — 5종 고정 목록은 그대로 두고 그 위에 얹은 탈출구 |
| 차단 | 부장님(`NOT_YET_BUILT`) | `supabase/blocks` 테이블 + RLS, TableDetail·Passport — RLS는 실 프로젝트 미검증 |
| PC 가로 스크롤 UX | 외국인 | `App.jsx`의 전역 wheel 리스너 — 마우스 휠이 가로 스크롤 행 위에 있으면 자동으로 좌우 스크롤로 바뀜, 컴포넌트 7곳 안 건드림 |
| SNS 확산 — 링크 미리보기 | 부장님 | 전역 OG는 `index.html`, **테이블별 미리보기는 8/4에 해결** — `api/table-og.js`(Vercel 함수)가 봇 UA에만 응답, 데이터는 `table_preview` RPC(anon에 카드 분량만 노출). og:image도 8/4에 해결 — 앱 아이콘을 그려 `public/icon-192.png`가 생기면서 래스터 자산 부재 문제가 사라짐. 요리 사진이 아니라 앱 마크인 것은 의도: 우리에게 그 밥상의 사진이 없고, 스톡 사진은 카드가 실제와 다른 걸 약속하는 셈 |
| 규칙 동의 받기 | 교수님("사용자 조항에 동의를 받던가") | `RulesConsent.jsx` + `policy/consent.js`, 첫 자리 요청·첫 상 차리기 직전에 1회, `profiles.rules_version`에 버전째로 기록 |
| **자리 요청 승인** | 부장님("초기에는 관리자의 승인 절차") | `policy/seatRequest.js` + `signups.status` + RLS `signups_decide_by_host`. 승인자는 운영자가 아니라 **호스트** — 아래 참고 |
| **매칭 확정 시간** | 교수님("매칭 확정은 얼마나 걸리며") | 식사 12시간 전까지, 답 없으면 만료되고 좌석 반환. `LAPSE_HOURS_BEFORE_MEAL` |
| **노쇼** | 교수님("상대가 No-Show일때는") | `policy/attendance.js` + `signups.attendance`. 평판 점수가 아니라 **기록의 정직성** — 패스포트가 안 온 사람을 "만난 사람"에 넣던 것을 멈춤 |

### 아직 없는 것 — 코드에서 확인함

| 요구 | 누가 | 상태 |
| --- | --- | --- |
| **이메일·전화번호** | 8/2 회의, 외국인 | **완료 (8/3~8/4)** — 회원가입에서 수집, 인증은 일부러 안 함(정보 파악 용도라는 회의 결정 그대로). `member_details` 테이블, RLS로 본인만 읽음, 팀은 대시보드로 |
| **프로필 사진** | 8/2 회의 | **완료 (8/4)** — 가입 중 등록(건너뛰기 가능), `avatars` 스토리지 버킷(2MB 캡, 본인 폴더만 쓰기), 256px 다운스케일 |
| **신고·후기** | 부장님 | **완료 (8/3 Meetup 배치)** — `reports`(팀만 읽음, select 정책 자체가 없음) + `reviews`(좌석당 한 줄, 별점 없음). `policy/report.js`·`policy/review.js`. **스키마 재실행 전까지 휴면** |
| **개인정보 처리방침 · 정식 이용약관** | (연락처 수집 시 필수) | 없음 — 행동 규칙 동의는 완료(위 표), 이건 법적 문서라 사람이 써야 함. **연락처를 실제로 수집하기 시작했으므로 파일럿 전 필수** |

### 확인이 필요한 항목 하나 — 해결됨

외국인 리뷰의 **"성별 선호 필터 긍정적으로 봄(안전 보장)"**은 이 저장소에
없는 기능이었습니다 (git 이력에도 없었음). 리뷰어가 다른 팀 프로토타입과
섞어서 봤는지는 끝내 확인하지 못했지만, 어느 쪽이든 답은 같다고 판단해
**만들었습니다** — 자기 신고 방식(`gender`, `nationality`와 동일하게 검증
안 됨), Tables 탭의 "여성 동석" 필터로 (`docs/superpowers/plans/2026-08-02-feedback-response-batch1.md` Group D).

이 필터에 관해 참고할 것 두 가지:

- **RLS.** `profiles.gender`는 `nationality`가 이미 가진 것과 똑같이 넓은
  읽기 정책(`profiles_read`)을 그대로 물려받습니다 — 새로운 노출이
  아니라 기존 정책을 상속한 것이고, 이 배치에서 일부러 정책을 건드리지
  않았습니다. 더 좁히려면 별도의 RLS/뷰 재설계가 필요하고, 이번 배치의
  범위 밖입니다.
- **범위.** 이 필터는 일부러 "여성" 하나로 고정된 단일 선호이지, 일반적인
  성별 선택기가 아닙니다. 리뷰어가 요청한 것도 딱 그 하나("show me tables
  where I won't be the only woman")입니다. `tableIncludesGender`
  (`src/domain/catalog/genders.js`)는 어떤 값이든 받으므로, 나중에
  일반화가 필요하면 데이터 모델이 아니라 UI만 바꾸면 됩니다.

### 교수님 지적 중 일부러 다르게 간 것 — 이제 절반은 반영됨

교수님은 **"No데이팅 앱"을 대문에서 빼고 사용자 조항(약관)에 넣어
동의를 받으라**고 했습니다. 원래는 앞부분만 따랐고(대문에서 뺌, 상세
화면에 `PURPOSE` 표시), **동의 자체가 없다**는 게 남은 구멍이었습니다.

**이제 동의를 받습니다.** 첫 자리 요청 직전과 첫 상 차리기 직전에
`RulesConsent`(`src/components/RulesConsent.jsx`)가 규칙 4개를 보여주고
동의 버튼을 눌러야 넘어갑니다. 한 번만 묻고, `profiles.rules_version` /
`rules_agreed_at`에 기록됩니다.

**위치는 여전히 교수님 말씀과 다릅니다** — 별도 약관 페이지가 아니라
흐름 안입니다. 이유는 그대로: 약관 페이지는 안 읽히고, 자리를 요청하기
직전이 실제로 읽히는 유일한 지점입니다. 다만 이제 "읽히기만 하고 동의는
없음"이 아니라 **읽히는 자리에서 동의를 받는** 형태입니다.

**버전을 붙인 이유**: `PURPOSE.version`을 올리면 이미 동의한 사람에게도
다시 묻습니다. boolean이면 규칙이 바뀌어도 옛날 동의가 그대로 유효한
것처럼 남는데, 그건 아무도 하지 않은 동의를 기록해두는 셈입니다.
(반대로 오타 수정 같은 건 버전을 올리지 마세요 — 쉼표 하나 때문에 다시
동의를 받으면 사람들이 안 읽고 누르는 습관이 생깁니다.)

**아직 아닌 것**: 이건 행동 규칙 동의지 개인정보 처리방침이나 정식
서비스 이용약관이 아닙니다. 그 두 개는 법적 문서라 사람이 써야 하고,
연락처를 받기 시작하면 반드시 필요합니다(위 ④ 참고).

### 승인 절차와 노쇼 — 한때 "결정 대기"였다가 만들어진 것

이 자리에는 **둘 다 사람이 정해줘야 해서 못 만든다**고 적혀 있었습니다.
그건 미루기였습니다. 정할 수 있는 것이었고, 정해서 만들었습니다.

**승인하는 사람은 호스트입니다.** 부장님이 말한 "관리자"를 운영 데스크로
읽으면 파일럿 첫날 아무도 상을 못 엽니다. 낯선 사람 셋과 불판을 나눌 사람이
누가 오는지 볼 자격과 이유가 가장 큽니다. 아무도 안 보는 결재함을 만드느니
이쪽이 낫습니다.

**확정 시간은 "식사 12시간 전까지"**입니다. 답이 없으면 요청이 만료되고
좌석이 돌아갑니다 — 저장하지 않고 식사 시각에서 계산합니다. 파일럿엔 그걸
써줄 배치 작업을 돌릴 데가 없고, **아무도 갱신 안 하는 상태값은 결국
거짓말**이 됩니다.

**노쇼는 결과가 있어야 장식이 아니라던 그 원칙**(`RESTRICTIONS` 주석의
*"a preference the app never acts on is decoration"*) 그대로입니다.
결과는 평판 점수가 아니라 **기록의 정직성**입니다 — 패스포트가 식사 시각만
지나면 그 상의 전원을 "만난 사람"에 넣고 있었고, 대기 중인 사람도 거절당한
사람도 안 온 사람도 거기 있었습니다. 이제 안 옵니다.

자세한 판단 근거는 `src/domain/policy/seatRequest.js`와
`attendance.js` 맨 위 주석에 있습니다. 특히 **대기 요청이 좌석을 잡는다**는
결정과 그 대가(침묵하는 호스트가 상을 얼림 → 그래서 만료가 있음).

### 기능이 아니라 답이 필요한 질문들

교수님이 UX 구상으로 물은 것 중 아직 답이 없는 것:

- 1인분을 팔지 않는 메뉴만 매칭되나, **2인분 파는 메뉴도 되나?**
  (코드는 `menu.minPeople`로 이미 판단하고 있으니, 카탈로그에 뭘 넣느냐의
  문제입니다 — 화면이 그 규칙을 말하지 않을 뿐)
- 3인 이상만 매칭 확정 / 식당 예약 연동 — 검토했나?
- 나눠 먹는 문화가 낯선 외국인에게 **어떻게 홍보할 것인가?**

답한 것: **매칭 확정 시간**(12시간), **노쇼**(위 참고).

부장님이 제안한 구현 수단 중 검토 안 된 것: **구글 인증 연계**(현재는
Supabase 익명 인증), 구글 지도 API, AI API 다국어.

---

## 5. 지금 할 수 있는 일 — 우선순위대로

### ⓪ 호스트 테이블을 공공외교 언어로 다시 쓰기 · 완료

위 4번의 첫 지적이었습니다. 기능은 있는데 그걸 설명하는 문장이 없었습니다.
`TABLE_KIND_LABEL.hosted.why`(`hosts.js`)에 "한국인 호스트가 자기 나라 음식을
당신 언어로 설명해주는 자리"라는 문장을 넣고, 테이블 상세(`TableDetail.jsx`)와
상 차리는 폼(`TableCreate.jsx`, 가이드를 하나라도 체크했을 때만)에 띄웁니다.
테이블 메이트 쪽에는 일부러 `why`를 안 넣었습니다 — 없는 교류를 있다고
말하는 셈이 되니까요.

### ① README.md를 밥친구로 다시 쓰기 · 완료

K-Food Map 설명이던 README를 밥친구 기준으로 전면 재작성했습니다.

`docs/DATA.md`와 `docs/EVIDENCE.md`는 아직 K-Food Map 시절 문서입니다. 다만
이쪽은 `src/data/verification.js`가 아직 살아서 쓰고 있으니 **지우지 말고**
어디까지 유효한지만 확인하세요.

### ② 죽은 파일 정리 · 완료

참조 0건을 확인하고 `temp.js`, `geocode_and_build.cjs`, `verify.cjs`를
삭제했습니다. `data/evidence/`(2개)는 `verification.js`가 아직 쓰므로
남겨뒀습니다.

### ③ 다크 모드 · 완료, 브라우저 실측까지 끝남

`src/index.css` 맨 끝에 `@media (prefers-color-scheme: dark) { :root { ... } }`
한 블록을 추가했습니다. 기존 토큰 39개 중 색상 토큰(중립·브랜드·Explore
에디토리얼)만 값을 분기했고 — `--radius-*`/`--shadow-sheet`은 색상이 아니라
분기 안 함, `--shadow-1`/`--shadow-2`는 분기함 — 선택자는 한 줄도 안
건드렸습니다. `index.html`에 `media` 속성이 다른 `theme-color` 메타 태그
두 개를 추가해 모바일 상태바도 같이 바뀝니다.

모든 텍스트 조합은 WCAG 2.1 공식(상대 휘도)으로 직접 계산해 확인했습니다 —
일반 텍스트 4.5:1, 큰 텍스트/UI 요소 3:1. `--green-strong`·`--orange-strong`처럼
"흰 글씨 얹은 배경"과 "배경 위 직접 텍스트" 두 역할을 겸하는 토큰은 한 값이
둘 다 4.5:1을 만족할 수 없어서 — 버튼 라벨(흰 글씨) 쪽을 우선하고 직접
텍스트 역할은 3:1대로 타협했습니다. 계산 근거는 `src/index.css`의 새 블록
주석에 실측값과 함께 적어뒀습니다.

**실측했고, 그 절충이 안 통했습니다.** 브라우저에서 375×812로, 트랜지션을
끄고 `getComputedStyle`로 재보니 네 화면에 걸쳐 **71건이 AA 미달**이었습니다.
`--green-strong`은 `--surface`에서 3.33:1, `--green-tint`에서 3.01:1까지
떨어졌습니다 — 민트 칩 위 초록 글씨, 즉 이 토큰이 가장 많이 쓰이는 바로 그
조합입니다.

그래서 절충을 접고 토큰을 쪼갰습니다. 배경은 `--green-strong`, 글자는
**`--green-ink`** (라이트에선 값이 같아 변화 없음, 다크에선 6.5~8.1:1).
`--orange-strong`도 같은 병이라 `--orange-ink`로 같이 갈랐습니다.

더 나빴던 건 **JSX 인라인에 박힌 색**이었습니다. 테마 변수를 볼 수 없어서
Passport 빈 화면이 흰 카드 + 흰 글씨 **1.15:1**이었습니다.

**지금은 다크·라이트 두 테마 × 전 화면(4탭 + 식당 상세 + 상 상세 + 회화
시트 + 퀴즈) AA 실패 0건입니다.**

재측정할 때 7번의 함정(백그라운드 탭에서 재면 트랜지션이 멈춰서 시작값이
나옴)은 그대로 적용됩니다. 그리고 측정기를 새로 쓸 거라면 두 가지를 넣으세요:
`aria-hidden` 요소는 제외할 것(선언된 장식은 대비 대상이 아님), 그리고
**그라디언트 배경은 건너뛸 것** — `backgroundColor`만 읽으면 그라디언트를
못 보고 뒤쪽 흰 페이지를 배경으로 잡아 멀쩡한 요소를 1:1로 오탐합니다.

### ④ 프로필 채우기 · 알러지는 완료, 이메일·전화·사진은 사람 결정 대기

회의와 외국인 리뷰가 같이 요구한 세 가지 중 하나만 막힌 게 없었습니다.

**완료 — 알러지 직접 입력.** 5종 고정 목록(`RESTRICTIONS`)은 그대로 두고,
그 위에 자유 텍스트 `profile.allergyNote`를 얹었습니다. 새우 알러지는
사실 `shellfish`로 이미 커버되지만, 견과류·참깨처럼 5종 밖에 있는 알러지는
지금까지 요청 메모(테이블마다 다시 입력)로만 전달됐습니다 — 이제 프로필에
한 번 적으면 매번 따라갑니다. `diets`와 동일하게 "메시지, 판정 아님" —
앱이 메뉴 재료와 대조해서 검증하지 않고, 그냥 호스트에게 그대로 전달만
합니다. `supabase/schema.sql`의 `signups.allergy_note`, `tableMapping.js`,
양쪽 백엔드, `TableDetail.jsx`의 "Who is going" 목록까지 다 연결됨.

**보류 — 이메일·전화번호, 프로필 사진.** 둘 다 일부러 안 만들었습니다.
연락처는 **받는 순간 개인정보 처리 근거가 필요**하고, 사진은 저장 위치·
용량 제한·모더레이션 같은 결정이 먼저 필요합니다. 국고가 들어간 공공외교
파일럿에서 이 둘은 코드 몇 줄로 끝날 결정이 아니라고 판단해 사람에게
넘겼습니다. `profile.js`에 필드를 추가하는 자체는 어렵지 않으니, 결정만
나면 바로 붙일 수 있습니다.

### ⑤ 퀴즈 문제 늘리기 · 완료 (16 → 24문제)

10개 요리 전부 이미 문제가 있었어서 "빈 요리"는 없었고, 문제는 물량 —
한 상에서 6문제가 뜨는데 일반 풀이 5개뿐이라 두 번째 방문이면 거의 다 본
문제였습니다. `encykorea.aks.ac.kr`·한국어 위키백과·기존 `facts-korea-dining`을
직접 열어 읽고 8문제를 새로 추가했습니다 (요리별 6개: 삼겹살 "IMF 삼겹살"
유행, 부대찌개 라면이 원래 없었다는 것, 간장게장이 실은 안 익힌다는 것,
닭갈비가 구이가 아니라 볶음이라는 것, 감자탕 2시간 끓이는 것, 백반의 문자
그대로 뜻; 일반 풀 2개: 금속 젓가락, 같은 냄비에 숟가락 같이 담그는 문화).
족발·곱창은 대조할 만한 새 사실을 못 찾아서 그대로 뒀습니다 — 억지로
채우지 않는다는 원칙 그대로. `sources.js`의 기존 출처 7개에 새 사실을
얹었고(같은 문서에서 더 읽은 것), `wikipedia-baekban-ko` 하나만 새로
등록했습니다. `src/content/quiz.js` 맨 위 주석의 두 규칙은 그대로 지켰고,
`quiz.test.mjs`가 소스 등록·중복 id·하지 매칭 전부 통과.

### ⑥ 안전 기능 · 차단은 완료, 나머지는 여전히 정책/서버 작업 대기

`src/content/safety.js`의 `NOT_YET_BUILT`에 세 개가 적혀 있었습니다. 숨긴 게
아니라 **화면에 "아직 없다"고 표시**하고 있던 항목이고, 그중 하나만 순수
코드+스키마 작업이라 진행했습니다 — 나머지 둘은 결정이 먼저 필요하다고
판단해 그대로 뒀습니다.

**완료 — 차단.** `supabase/blocks` 테이블 + RLS 두 겹으로 구현했습니다:

1. **내 화면에서 안 보이게** (`src/domain/policy/blocking.js`) — 순수
   클라이언트 필터. 내가 누굴 차단했는지는 RLS로 나만 읽을 수 있고
   (`blocks_select_own`), Tables·찾는 밥상 목록에서 그 호스트 상이 사라집니다.
2. **상대가 내 상에 못 들어오게** — `signups_insert_own` RLS를 확장해서,
   `blocked_id = auth.uid() and blocker_id = (그 상의 host_id)`인 경우 좌석
   INSERT 자체를 막습니다. 클라이언트가 아니라 DB가 막는 이유: 막히는
   당사자의 브라우저가 그 검사를 실행 안 할 수도 있으니까요.

**일부러 안 한 것**: 이미 잡은 좌석은 안 건드림(소급 안 함), "차단당했다"는
사실 자체를 상대에게 알 방법이 없게 함(RLS select가 blocker_id=본인만 —
차단 사실을 알리는 게 오히려 위험을 키울 수 있다는 판단, 대부분의 플랫폼도
이렇게 함). TableDetail의 "Who is going"에서 호스트는 각 게스트를,
게스트는 호스트를 차단할 수 있고(둘 다 확인창 있음), Passport에 "Blocked"
목록 + 해제 버튼도 있습니다. 테스트 8개 추가(`blocking.test.mjs` 5개,
`tableMapping.test.mjs` 블록 매핑 3개) — 로컬(localStorage) 백엔드는
tableRepository.js 전체가 그렇듯 직접 테스트가 없어서 수동 스모크 스크립트로
create/list/idempotent/delete/필터링을 확인했습니다. 실 Supabase 프로젝트에
이번 배치까지의 스키마를 재적용해야 실제로 작동합니다 (배포 순서는 README
참고) — RLS 정책(특히 `signups_insert_own` 확장)은 실제 프로젝트에서
아직 검증 못 했습니다, 재적용 후 두 계정으로 직접 확인하세요.

**여전히 보류 — 평판(매너온도류)만.** 어뷰징 방지 설계와 "누가 감시하냐"는
결정이 먼저 필요해서 일부러 안 만들었고, `policy/attendance.js` 머리 주석에
그 이유가 적혀 있습니다. 인앱 신고는 이후 완료됐습니다(8/3 Meetup 배치,
`reports` 테이블 — 위 "완료" 표 참고). 승인 절차(`signups_decide_by_host`,
승인자는 호스트)와 노쇼 기록(`signups.attendance`)도 그 뒤에 완료 — 이
문단보다 위의 완료 표가 최신입니다.

긴급 신고·도움은 여전히 SafetySheet의 번호들이고, 인앱 신고는 팀이
Supabase 대시보드에서 `reports` 테이블을 읽는 방식입니다. **파일럿이 도는
동안 사람이 그 테이블을 봐야 한다는 약속이 접수 문구에 적혀 있습니다**
("a person will read it" — `policy/report.js`의 REPORT_RECEIPT, 과잉 약속
단어는 테스트가 차단).

---

## 6. 막혀 있는 것 — 사람이 결정해야 진행됨

원격 작업자가 혼자 뚫을 수 없습니다. 손대지 말고 넘기세요.

| 항목 | 필요한 것 | 준비되면 |
| --- | --- | --- |
| 인증 호스트 | 실제 명단 | `host_verified` SQL 생성 |
| 제휴 식당 | 제휴 확정 | 카탈로그 반영 |
| Supabase 테스트 데이터 | 지워도 되는지 확인 | 아래 SQL |

파일럿 전에 테스트 상을 비울 때 (**되돌릴 수 없습니다**):

```sql
delete from public.signups; delete from public.tables; delete from public.profiles;
```

---

## 7. 구조에서 알아둘 것

**계층:** `Policy → Projection → Capability → Entity → Value Object`
(`src/domain/`). 판단은 policy로 올리세요. 컴포넌트 안에 규칙을 쓰면 테스트가
닿지 않습니다 — "결과가 하나도 없을 때만 상 열기를 권한다"는 판단이 컴포넌트에
있다가 틀려서 `src/domain/policy/matching.js`로 옮긴 전례가 있습니다.

**저장소 이음매:** `src/data/tableRepository.js` 하나가 localStorage ⇄ Supabase를
가립니다. 백엔드를 바꿀 일이 있으면 이 파일만 봅니다.

**localStorage 키:** `bapchingu-tables`, `bapchingu-signups`, `bapchingu-profile`,
`kfm-bookmarks`, `kfm-markets`, `kfm-experiences`, `kfm-prologue`
(`kfm-` 접두사는 K-Food Map 시절 이름이 남은 것입니다).

**테스트:** Node 24 내장 러너, 의존성 0. `src/**/*.test.mjs`, 22개 파일 243개.
로컬(localStorage) 백엔드인 `tableRepository.js`는 직접 테스트가 없습니다 —
도메인 계층(순수 함수)을 통해서만 검증하고, 백엔드 자체는 필요할 때 수동
스모크 스크립트로 확인하는 게 이 저장소의 방식입니다.

### 함정 두 개 — 둘 다 실제로 당했습니다

**대비를 측정할 땐 트랜지션을 끄세요.** `document.visibilityState === "hidden"`
이면 애니메이션 타임라인이 멈춰서 `getComputedStyle`이 **시작값**을 돌려줍니다.
없는 버그를 한참 쫓았습니다.

**두 탭은 두 사람이 아닙니다.** 같은 오리진의 탭은 Supabase 세션을 공유합니다.
멀티유저 테스트는 실제 로그아웃 사이클로 하세요.

---

## 8. 배포

```bash
git push        # master → test/main → Vercel
```

`origin`은 제거했고 `master`는 `test/main`을 추적합니다. Vercel 환경변수에
`VITE_SUPABASE_URL`과 `VITE_SUPABASE_ANON_KEY`가 들어 있어야 프로덕션이 공유
저장소를 씁니다.

전체 배치는 `docs/where-this-deploys.md`. **옆 폴더의 K-Food Map은 다른 앱이고
다른 URL이며, 두 앱은 초기 git 이력을 공유합니다.** 그게 사고가 가능했던
이유입니다.
