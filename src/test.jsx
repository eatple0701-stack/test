import React from 'react'; import { renderToString } from 'react-dom/server'; import RestaurantDetail from './src/components/RestaurantDetail.jsx'; import { restaurants } from './src/data/restaurants.js'; restaurants.forEach(r => { try { renderToString(React.createElement(RestaurantDetail, { restaurant: r })); } catch (e) { console.error('Error on restaurant:', r.name); console.error(e); process.exit(1); } }); console.log('All good!');  
